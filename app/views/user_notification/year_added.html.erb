<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h4>Bonjour, <%= @user.firstname %> <%= @user.lastname %></h4>
    <p>
      Votre abonnement au site Carnet d'Adresses CI a été prolongé de 1 an.
    </p>

    <p>
      La date d'expiration de votre abonnement est passée de: <%= @last_registration.expires_at.strftime("%d-%m-%Y") %> à <%= @expires_at.strftime("%d-%m-%Y") %>
    </p>

    <p>
      A bientôt.
    </p>
  </body>
</html>
