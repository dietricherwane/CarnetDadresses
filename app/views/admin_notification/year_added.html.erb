<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h4>Bonjour, </h4>
    <p>
      L'abonnement de <%= @user.firstname %> <%= @user.lastname %> [ <%= @user.email %> ] au site Carnet d'Adresses CI a été prolongé de 1 an.
    </p>

    <p>
      La date d'expiration de son abonnement est passée de: <%= @last_registration.expires_at.strftime("%d-%m-%Y") %> à <%= @expires_at.strftime("%d-%m-%Y") %>
    </p>

    <p>
      A bientôt.
    </p>
  </body>
</html>
